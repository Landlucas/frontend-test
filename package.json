{
  "name": "beer-server",
  "version": "1.0.0",
  "description": "Beer server",
  "main": "server.js",
  "dependencies": {
    "bootstrap": "^4.4.1",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "npm-run-all": "^4.1.5",
    "parcel": "^1.12.4",
    "sass": "^1.26.5",
    "stylelint": "^13.3.3",
    "stylelint-config-recommended-scss": "^4.2.0",
    "stylelint-config-standard": "^20.0.0",
    "stylelint-scss": "^3.17.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "parcel build ./public/src/js/main.js --out-dir ./public -o client.js --cache-dir .cache",
    "develop": "parcel watch ./public/src/js/main.js --out-dir ./public -o client.js --cache-dir .cache",
    "develop-start": "run-p develop start"
  },
  "author": "",
  "license": "ISC"
}
